@using Stranded.Models.ViewModels
<!DOCTYPE html>
<body>
    <div class="Gui">
        <table align="center">
            @for (int i = 0; i < 14; i++)
            {
                <tr>
                    @for (int b = 0; b < 14; b++)
                    {
                        <td><img src="~/images/Map/pixil-frame-0.png" /></td>
                    }
                </tr>
            }
        </table>
    </div>
</body>